# Development Environment Configuration
# Used when running: npm run dev

# Local API URL (proxied through Vite)
VITE_API_URL=/api

# Direct backend URL (if not using proxy)
# VITE_API_URL=https://localhost:5001/api
